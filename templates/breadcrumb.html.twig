{% if breadcrumb %}
<nav class="breadcrumb" role="navigation">
	<ol>
		{% for item in breadcrumb %}
		<li>
			{% if item.url %}
			<a href="{{ item.url }}">{{ item.text }}</a>
			{% else %}
			{{ item.text }}
			{% endif %}
		</li>
		{% endfor %}
	</ol>
</nav> <!-- /.breadrcumb -->
{% endif %}